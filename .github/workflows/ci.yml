name: 'CI'

on:
  pull_request:
    paths-ignore:
      - '**.md'
  push:
    branches:
      - main
      - 'releases/*'
    paths-ignore:
      - '**.md'

jobs:
  test:
    name: 🔍 Test on [${{ matrix.os }}, Node ${{ matrix.node }}]
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        node: ['12.x', '14.17.0']
    steps:
      - name: 🛎️ Checkout repo
        uses: actions/checkout@v2
      - name: 🖥️ Setup Node.js ${{ matrix.node }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node }}
      - name: 👨‍💻 Install dependencies
        run: npm ci
      - name: 🔍 Test
        env:
          INPUT_ENDPOINT: ${{ secrets.INPUT_ENDPOINT }}
          INPUT_USERNAME: ${{ secrets.INPUT_USERNAME }}
          INPUT_PASSWORD: ${{ secrets.INPUT_PASSWORD }}
          INPUT_DOMAIN_ID: ${{ secrets.INPUT_DOMAIN_ID }}
        run: npm run test:ci
      - name: ⬆️ Upload coverage report to Coveralls
        continue-on-error: true
        # https://github.com/coverallsapp/github-action
        uses: coverallsapp/github-action@v1.1.2
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

  action-development:
    name: 📈 Run ackee-action@HEAD
    runs-on: ubuntu-latest
    steps:
      - name: 🛎️ Checkout repo
        uses: actions/checkout@v2
      - name: ⬇️ Fetch data from Ackee
        id: ackee
        uses: ./
        with:
          endpoint: ${{ secrets.INPUT_ENDPOINT }}
          username: ${{ secrets.INPUT_USERNAME }}
          password: ${{ secrets.INPUT_PASSWORD }}
          domain_id: ${{ secrets.INPUT_DOMAIN_ID }}
          num_top_pages: '5'
          query: |
            query getTop10BrowsersOfLast6Months {
              domains {
                statistics {
                  browsers(sorting: TOP, type: WITH_VERSION, range: LAST_6_MONTHS, limit: 10) {
                    id
                    count
                  }
                }
              }
            }
      - name: 📨 Send report to Telegram
        uses: appleboy/telegram-action@v0.1.1
        # https://github.com/appleboy/telegram-action
        env:
          HTML_MESSAGE: ${{ steps.ackee.outputs.message }}
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          format: html
          disable_web_page_preview: true
          # photo: ./dist/house.png
          # photo: ./images/clock.png
          # document: ./images/ray.png
          message: ${{ env.HTML_MESSAGE }}
      - name: 📨 Send report to Slack
        uses: pullreminders/slack-action@v1.0.7
        # https://github.com/abinoda/slack-action
        env:
          HTML_MESSAGE: ${{ steps.ackee.outputs.message }}
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          SLACK_CHANNEL_ID: ${{ secrets.SLACK_CHANNEL_ID }}
        with:
          args: '{\"channel\":\"${{ env.SLACK_CHANNEL_ID }}\",\"text\":\"${{ env.HTML_MESSAGE }}\"}'

  action-main:
    name: 📈 Run ackee-action@main
    if: ${{ 'foo' == 'bar' }} # TODO: remove this
    runs-on: ubuntu-latest
    steps:
      - name: 🛎️ Checkout repo
        uses: actions/checkout@v2
      - name: ⬇️ Fetch data from Ackee
        id: ackee
        uses: jackdbd/ackee-action@main
        with:
          endpoint: ${{ secrets.INPUT_ENDPOINT }}
          username: ${{ secrets.INPUT_USERNAME }}
          password: ${{ secrets.INPUT_PASSWORD }}
          domain_id: ${{ secrets.INPUT_DOMAIN_ID }}
          num_top_pages: '5'
      - name: Dump data
        run: echo "${{ steps.ackee.outputs.data }}"
      - name: Dump message
        run: echo "${{ steps.ackee.outputs.message }}"

  action-v1-1-0:
    name: 📈 Run ackee-action@v1.1.0
    if: ${{ 'foo' == 'bar' }} # TODO: remove this
    runs-on: ubuntu-latest
    steps:
      - name: 🛎️ Checkout repo
        uses: actions/checkout@v2
      - name: ⬇️ Fetch data from Ackee
        id: ackee
        uses: jackdbd/ackee-action@v1.1.0
        with:
          endpoint: ${{ secrets.INPUT_ENDPOINT }}
          username: ${{ secrets.INPUT_USERNAME }}
          password: ${{ secrets.INPUT_PASSWORD }}
          domain_id: ${{ secrets.INPUT_DOMAIN_ID }}
          num_top_pages: '5'
      - name: Dump data
        run: echo "${{ steps.ackee.outputs.data }}"
      - name: Dump message
        run: echo "${{ steps.ackee.outputs.message }}"

  release:
    name: 📣 Release
    needs: [test, action-main]
    runs-on: ubuntu-latest
    steps:
      - name: 🛎️ Checkout repo
        uses: actions/checkout@v2
      - name: 🖥️ Setup Node.js
        uses: actions/setup-node@v1
        with:
          node-version: '12.x'
      - name: 👨‍💻 Install dependencies
        run: npm ci
      - name: 📦 Bundle action
        run: npm run bundle
      - name: 📣 Release with semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run release
